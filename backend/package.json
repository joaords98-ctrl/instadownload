{
  "name": "ig-media-official-backend",
  "version": "0.4.1",
  "private": true,
  "description": "Backend para baixar mídia própria/autorizada do Instagram via Graph API sem recompressão.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check api/[...path].js"
  },
  "engines": {
    "node": ">=18"
  }
}
