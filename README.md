# Extensão Chrome

Carregue esta pasta em `chrome://extensions` usando **Carregar sem compactação**.

Use o botão **Detectar mídia** para fotos e vídeos expostos diretamente pela página.
Use **Baixar via API oficial** quando o vídeo aparecer como `blob/stream`, com o backend local rodando.
